#include <bits/stdc++.h>
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    string s, t;
    cin >> s >> t;
    cout << s.length() << " " << t.length() << endl;
    cout << s << " " << t << endl;
    return 0;
}