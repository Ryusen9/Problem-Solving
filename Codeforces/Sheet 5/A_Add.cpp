#include <bits/stdc++.h>
using namespace std;
int add(long long a, long long b) {
    long long sum = 0;
    return sum = a + b;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    long long a, b;
    cin >> a >> b;
    cout << add(a, b) << endl;
    return 0;
}